server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  # application:
  #   name: let-me-cook
  datasource:
    url: "jdbc:mysql://localhost:3306/identity_service"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update # tự động tạo table và update mỗi khi có lệnh sql
    show-sql: true # giúp debug các lệnh sql
